# Frontend environment (Vite). All public — never put secrets here.
# Copy to `.env.local` for local development.

VITE_API_BASE_URL="http://localhost:8000/api/v1"
VITE_APP_NAME="AI Career Interview Platform"
VITE_APP_ENV="local"
