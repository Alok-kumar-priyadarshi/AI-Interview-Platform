# ---------------------------------------------------------------------------
# Development & test dependencies. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
# ---------------------------------------------------------------------------
-r requirements.txt

# --- Testing ----------------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
aiosqlite==0.20.0                 # async SQLite driver for fast in-memory tests
asgi-lifespan==2.1.0              # drive FastAPI lifespan in tests

# --- Tooling ----------------------------------------------------------------
ruff==0.8.4                       # linting + import sorting
black==24.10.0                    # formatting
mypy==1.14.0                      # static type checking
